# Webhook URL for lead submissions
WEBHOOK_URL=https://your-webhook-url.com/leads

# Facebook CAPI Configuration (already set in code, but can be overridden via env)
# FB_PIXEL_ID=your_pixel_id
# FB_ACCESS_TOKEN=your_access_token
